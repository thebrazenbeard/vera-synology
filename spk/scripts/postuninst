#!/bin/sh
set -eu
PY=/var/packages/python311/target/bin/python3.11
if [ "${SYNOPKG_PKG_STATUS:-}" = "UNINSTALL" ]; then
  "$PY" "$SYNOPKG_PKGDEST/bin/veramesh_lifecycle.py" retire-uninstall
fi
# Mesh/trust/inbox durable data remains untouched.
exit 0
